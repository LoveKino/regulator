#pragma once

#include "regularExp.h"

namespace sfsm {
/*
class commonTokenReg {
private:
  const string hexadecimalRegExpStr = "([0-9]|[a-f]|[A-F])";
  const string unicodeRegExpStr = "u" + hexadecimalRegExpStr +
                                  hexadecimalRegExpStr + hexadecimalRegExpStr +
                                  hexadecimalRegExpStr;
  const string controlCharacterStr =
      "\\(\"|\\|/|b|f|n|r|t|" + unicodeRegExpStr + ")";

public:
  const string jsonStringExpStr = "\"\"";
  const string jsonNumberExpStr = "";
  const RegularExp jsonStringExp(jsonStringExpStr);
  const RegularExp jsonNumberExp(jsonNumberExpStr);
} // namespace commonTokenReg
*/
} // namespace sfsm
